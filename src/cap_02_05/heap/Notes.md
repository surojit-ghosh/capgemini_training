### Heap 

- A heap is a specialized binary-tree-based data structure that satisfies the heap property.
- Max heap, Min heap

### Find frequency
{ 1, 1, 2, 1, 2, 3, 4}
- dump all elements in a map with their frequency
- create a priority queue (max-heap) based on frequency
- get the top k elements from the max-heap that will be the k most frequent elements